{% extends 'base3.html.twig' %}

{% block title %}New Appointment{% endblock %}

{% block body %}

 <main class="relative h-full max-h-screen transition-all duration-200 ease-in-out xl:ml-68 rounded-xl">
<div class="flex flex-wrap -mx-3">
          <div class="flex-none w-full max-w-full px-3">
            <div class="relative flex flex-col min-w-0 mb-6 break-words bg-white border-0 border-transparent border-solid shadow-xl dark:bg-slate-850 dark:shadow-dark-xl rounded-2xl bg-clip-border">
              <div class="p-6 pb-0 mb-0 border-b-0 border-b-solid rounded-t-2xl border-b-transparent">
                <h2 class="dark:text-white"> Add New Appointment</h2>

      <div class="text-6xl text-center pb-9">

    <a href="{{ path('app_appointment_index') }}" class="bg-gradient-to-tl from-emerald-500 to-teal-400 px-2.5 text-xs rounded-1.8 py-1.4 inline-block whitespace-nowrap text-center align-baseline font-bold uppercase leading-none text-white">back to list</a> 
     </div>
     {{ include('appointment/_form.html.twig') }}
     <tbody>
     {% for appointment in appointment %}
     <tr>
      <td class="p-2 align-middle bg-transparent border-b dark:border-white/40 whitespace-nowrap shadow-transparent">
                          
                          <a href="{{ path('app_appointment_show', {'id': appointment.id}) }}" class="text-xs font-semibold leading-tight dark:text-white dark:opacity-80 text-slate-400">show </a>
                          <a href="{{ path('app_appointment_show', {'id': appointment.id}) }}" class="text-xs font-semibold leading-tight dark:text-white dark:opacity-80 text-slate-400">edit </a>
                        </td>
                      </tr>
                       {# {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr> #}
        {% endfor %}
        </tbody>
     </div>
     </main>
{% endblock %}
